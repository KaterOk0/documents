package by.st.repository;

import by.st.model.QueryType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface QueryTypeRepository extends JpaRepository<QueryType, Integer> {
}
